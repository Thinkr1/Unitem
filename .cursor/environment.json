{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "cd UI && npm install",
  "terminals": [
    {
      "name": "ui",
      "command": "cd UI && npm run dev"
    },
    {
      "name": "engine",
      "command": "echo 'Engine not built yet — see ARCHITECTURE.md §10. When ready: cd engine && pip3 install -e . && python3 -m unitem.api  # FastAPI on :8787'"
    }
  ]
}
