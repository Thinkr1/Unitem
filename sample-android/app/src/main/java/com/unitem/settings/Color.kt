package com.unitem.settings

import androidx.compose.ui.graphics.Color

object AppColor {
    // Demo: stale hardcoded primary for FLAG scenario (should use token #1D4ED8 after propagate)
    val Primary = Color(0xFF2563EB)
    val Error = Color(0xFFDC2626)
}

object AppSpacing {
    const val Sm = 8
    const val Md = 16
    const val Lg = 24
}
