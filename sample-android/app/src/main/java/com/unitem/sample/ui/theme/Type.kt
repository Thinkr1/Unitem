package com.unitem.sample.ui.theme

import androidx.compose.ui.text.font.FontFamily

// Brand typeface (bundled font in the real app; default here keeps the sample lean).
val SpaceGrotesk = FontFamily.Default
